#include <stdio.h>

int fibonacci(int n) {
    if (n <= 1) {
        return n; // Base case: return n if n <= 1
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2); // Recursively calculate Fibonacci series
    }
}

int main() {
    int num;
    printf("Enter the number of terms for the Fibonacci series: ");
    scanf("%d", &num);

    if (num <= 0) {
        printf("Number of terms should be greater than zero.\n");
    } else {
        printf("Fibonacci Series up to %d terms:\n", num);
        for (int i = 0; i < num; i++) {
            printf("%d ", fibonacci(i));
        }
        printf("\n");
    }
    printf("name:nanda kishor\n");
    printf("regno:192324048\n");

    return 0;
}
C:\Users\mrrag\Pictures\Screenshots\Screenshot 2023-12-21 100019.png
